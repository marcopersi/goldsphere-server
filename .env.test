# Test Environment Configuration
NODE_ENV=test
PORT=0
JWT_SECRET=test-jwt-secret-key-for-testing-only-min-32-chars
JWT_EXPIRES_IN=1h

# Test Database (you can modify these for your test setup)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=goldsphere_test
DB_USER=postgres
DB_PASSWORD=test_password

# Disable rate limiting in tests
RATE_LIMIT_MAX_REQUESTS=1000
RATE_LIMIT_WINDOW_MS=60000

# Test CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3333

# Disable some features in tests
API_DOCS_ENABLED=false
LOG_LEVEL=error
