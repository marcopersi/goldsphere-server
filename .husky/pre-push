echo "🔍 Running pre-push checks..."

# Run unit and contract tests (integration tests require running DB)
echo "🧪 Running unit and contract tests..."
bun run test
if [ $? -ne 0 ]; then
  echo "❌ Tests failed. Please fix the failing tests and try again."
  exit 1
fi

echo "✅ All pre-push checks passed!"
